<Window x:Class="UserPermissions.GroupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UserPermissions"
        mc:Ignorable="d"
        Title="GroupWindow" Height="450" Width="800">
    <Grid Background="DarkCyan">
        <StackPanel Orientation="Horizontal" Height="35" VerticalAlignment="Top" HorizontalAlignment="Center">
            <Button Content="Add to group" MinWidth="100" Click="AddToGroupBtn"/>
            <Button Content="Create group" MinWidth="100" Click="CreateGroupBtn" Margin="10,0,0,0"/>
            <Button Content="Delete group" MinWidth="100" Click="DeleteGroupBtn" Margin="10,0,0,0"/>
        </StackPanel>

        <Frame x:Name="GroupWin" Margin="0,35,0,0" NavigationUIVisibility="Hidden" />
        <Label x:Name="groupLabel" VerticalAlignment="Center" Content="Use the navigation button at the top to move between windows" HorizontalAlignment="Center" FontSize="24"/>
    </Grid>
</Window>
